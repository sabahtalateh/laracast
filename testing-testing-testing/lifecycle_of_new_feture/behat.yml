default:
    extensions:
        Laracasts\Behat:
            # env_path: .env.behat
        Behat\MinkExtension:
            laravel: ~
            show_cmd: '/usr/bin/google-chrome %s'

#            browser_name: 'chrome'
            base_url: 'http://localhost:8000'
            sessions:
                selenium2:
                    selenium2: ~
#            sessions:
#                default_session: laravel
